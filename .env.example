# Postgres connection URL (optional). When set, the app uses Postgres instead of file store.
# Get a URL from Neon, Vercel Postgres, Supabase, Railway, etc. See docs/POSTGRES_SETUP.md.
POSTGRES_URL=postgresql://user:password@host:port/database?sslmode=require

# Clerk (required for professional auth). Create an application at https://dashboard.clerk.com.
# Client (Vite): publishable key, exposed to the browser.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
# Server: secret key, never expose to the client.
CLERK_SECRET_KEY=sk_test_...

# Resend (optional). When set, confirmation emails are sent after booking. Reply-To = professional when set.
RESEND_API_KEY=re_...
# For testing: use resend.dev (default). Resend only allows sending TO your account email until you verify a domain.
# Leave unset or set to onboarding@resend.dev; for production later, verify a domain at resend.com/domains and set e.g. bookings@yourdomain.com
EMAIL_FROM=Lesson Scheduler <onboarding@resend.dev>
# Dev only: send all booking emails to this address (e.g. your Resend account email). Subject lines get [Dev] and real recipient shown so you can test without a verified domain. Unset in production.
# EMAIL_DEV_OVERRIDE=you@example.com
